<x-app-layout><x-slot name="header"><h1 class="text-2xl font-black">Booking requests</h1></x-slot><div class="space-y-4">@forelse ($bookings as $booking)<article class="rounded-3xl border border-white/10 p-6"><div class="flex flex-col justify-between gap-5 md:flex-row"><div><div class="flex flex-wrap gap-3"><h2 class="text-xl font-black">{{ $booking->member->name }}</h2><span class="tag">{{ ucfirst($booking->status) }}</span><span class="tag text-lime-300">{{ $booking->program_type }}</span></div><p class="mt-3 text-lime-300">{{ $booking->requested_datetime->format('d M Y, H:i') }}</p><p class="mt-3 text-sm text-stone-400">{{ $booking->notes ?: 'No notes supplied.' }}</p></div><form method="POST" action="{{ route('trainer.bookings.update', $booking) }}" class="flex items-end gap-2">@csrf @method('PATCH')<select name="status" class="rounded-xl border-white/10 bg-black">@foreach (['accepted','declined','completed','cancelled'] as $status)<option value="{{ $status }}" @selected($booking->status === $status)>{{ ucfirst($status) }}</option>@endforeach</select><button class="rounded-xl bg-lime-400 px-4 py-2.5 font-black text-black">Update</button></form></div></article>@empty<p class="text-stone-500">No booking requests yet.</p>@endforelse</div></x-app-layout>

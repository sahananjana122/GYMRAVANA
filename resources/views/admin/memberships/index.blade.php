<x-app-layout><x-slot name="header"><h1 class="text-2xl font-black">Membership tiers</h1></x-slot><div class="grid gap-6 lg:grid-cols-3">@foreach ($tiers as $tier)<form method="POST" action="{{ route('admin.memberships.update', $tier) }}" class="space-y-4 rounded-3xl border border-white/10 p-6">@csrf @method('PATCH')<h2 class="text-xl font-black">{{ $tier->name }} <span class="text-sm text-stone-500">· {{ $tier->member_profiles_count }} members</span></h2><div><label class="form-label">Name</label><input name="name" value="{{ $tier->name }}" class="form-input"></div><div class="grid grid-cols-2 gap-3"><div><label class="form-label">Price</label><input type="number" step="0.01" name="price" value="{{ $tier->price }}" class="form-input"></div><div><label class="form-label">Period</label><input name="billing_period" value="{{ $tier->billing_period }}" class="form-input"></div></div><div><label class="form-label">Features, one per line</label><textarea name="features_text" rows="5" class="form-input">{{ implode("\n", $tier->features) }}</textarea></div><label class="flex gap-2 text-sm"><input type="checkbox" name="is_featured" value="1" @checked($tier->is_featured)> Featured</label><label class="flex gap-2 text-sm"><input type="checkbox" name="is_active" value="1" @checked($tier->is_active)> Active</label><button class="w-full rounded-full bg-lime-400 px-4 py-3 font-black text-black">Save tier</button></form>@endforeach</div><section class="mt-10 rounded-3xl border border-white/10 p-6"><h2 class="text-xl font-black">Member tier assignments</h2><div class="mt-5 grid gap-3">@foreach ($members as $member)<form method="POST" action="{{ route('admin.members.tier', $member) }}" class="flex flex-col justify-between gap-3 rounded-2xl bg-white/[.035] p-4 sm:flex-row sm:items-center">@csrf @method('PATCH')<div><strong>{{ $member->name }}</strong><small class="block text-stone-500">{{ $member->email }}</small></div><div class="flex gap-2"><select name="membership_tier_id" class="rounded-xl border-white/10 bg-black text-sm">@foreach ($tiers as $tier)<option value="{{ $tier->id }}" @selected($member->memberProfile?->membership_tier_id === $tier->id)>{{ $tier->name }}</option>@endforeach</select><button class="rounded-xl border border-lime-400 px-4 py-2 text-sm font-bold text-lime-300">Assign</button></div></form>@endforeach</div></section></x-app-layout>

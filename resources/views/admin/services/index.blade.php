<x-app-layout><x-slot name="header"><h1 class="text-2xl font-black">Body and Mind services</h1></x-slot><div class="grid gap-8 lg:grid-cols-2">@foreach ($categories as $category)<section><h2 class="text-2xl font-black text-lime-300">{{ $category->name }}</h2><div class="mt-5 space-y-4">@foreach ($category->services as $service)<form method="POST" action="{{ route('admin.services.update', $service) }}" class="space-y-4 rounded-3xl border border-white/10 p-6">@csrf @method('PATCH')<input type="hidden" name="service_category_id" value="{{ $category->id }}"><div><label class="form-label">Name</label><input name="name" value="{{ $service->name }}" class="form-input"></div><div><label class="form-label">Summary</label><input name="summary" value="{{ $service->summary }}" class="form-input"></div><div><label class="form-label">Description</label><textarea name="description" rows="4" class="form-input">{{ $service->description }}</textarea></div><div class="grid grid-cols-3 gap-3"><input name="level" value="{{ $service->level }}" class="form-input" placeholder="Level"><input name="equipment" value="{{ $service->equipment }}" class="form-input" placeholder="Equipment"><input type="number" name="duration_minutes" value="{{ $service->duration_minutes }}" class="form-input" placeholder="Minutes"></div><label class="flex gap-2 text-sm"><input type="checkbox" name="is_active" value="1" @checked($service->is_active)> Active</label><button class="rounded-full bg-lime-400 px-5 py-2.5 font-black text-black">Save service</button></form>@endforeach</div></section>@endforeach</div></x-app-layout>

<x-app-layout><x-slot name="header"><h1 class="text-2xl font-black">Products and stock</h1></x-slot><div class="grid gap-8 lg:grid-cols-[1fr_320px]"><section><div class="grid gap-5 md:grid-cols-2">@foreach ($products as $product)<form method="POST" action="{{ route('admin.products.update', $product) }}" class="space-y-4 rounded-3xl border border-white/10 p-6">@csrf @method('PATCH')<h2 class="text-xl font-black">{{ $product->name }}</h2><div><label class="form-label">Category</label><select name="product_category_id" class="form-input">@foreach ($categories as $category)<option value="{{ $category->id }}" @selected($product->product_category_id === $category->id)>{{ $category->name }}</option>@endforeach</select></div><div><label class="form-label">Name</label><input name="name" value="{{ $product->name }}" class="form-input"></div><div><label class="form-label">Description</label><textarea name="description" rows="3" class="form-input">{{ $product->description }}</textarea></div><div class="grid grid-cols-2 gap-3"><div><label class="form-label">Price</label><input type="number" step="0.01" name="price" value="{{ $product->price }}" class="form-input"></div><div><label class="form-label">Stock</label><input type="number" name="stock" value="{{ $product->stock }}" class="form-input"></div></div><input type="hidden" name="image_path" value="{{ $product->image_path }}"><label class="flex gap-2 text-sm"><input type="checkbox" name="is_active" value="1" @checked($product->is_active)> Active</label><button class="rounded-full bg-lime-400 px-5 py-2.5 font-black text-black">Save product</button></form>@endforeach</div></section><aside class="space-y-5"><form method="POST" action="{{ route('admin.product-categories.store') }}" class="rounded-3xl border border-white/10 p-5">@csrf<h2 class="font-black">Add category</h2><input name="name" class="form-input mt-4" placeholder="Category name"><button class="mt-3 w-full rounded-full border border-lime-400 px-4 py-2 text-sm font-bold text-lime-300">Create category</button></form><form method="POST" action="{{ route('admin.products.store') }}" class="space-y-4 rounded-3xl border border-white/10 p-5">@csrf<h2 class="font-black">Add product</h2><select name="product_category_id" class="form-input"><option value="">Category</option>@foreach ($categories as $category)<option value="{{ $category->id }}">{{ $category->name }}</option>@endforeach</select><input name="name" class="form-input" placeholder="Product name"><textarea name="description" class="form-input" rows="3" placeholder="Description"></textarea><input type="number" step="0.01" name="price" class="form-input" placeholder="Price"><input type="number" name="stock" class="form-input" placeholder="Stock"><input name="image_path" class="form-input" placeholder="Optional image URL/path"><label class="flex gap-2 text-sm"><input type="checkbox" name="is_active" value="1" checked> Active</label><button class="w-full rounded-full bg-lime-400 px-4 py-3 font-black text-black">Create product</button></form></aside></div></x-app-layout>

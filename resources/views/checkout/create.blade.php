@extends('layouts.public')
@section('title', 'Checkout')
@section('content')
<main class="mx-auto max-w-3xl px-5 py-20 sm:px-8"><a href="{{ route('cart.index') }}" class="text-sm font-bold text-lime-300">← Back to cart</a><p class="section-kicker mt-10">Guest checkout</p><h1 class="page-title">Where should we send it?</h1><p class="page-lead">Submitting creates a pending order. No card or payment information is requested.</p><form method="POST" action="{{ route('checkout.store') }}" class="mt-10 space-y-5 rounded-[2rem] border border-white/10 bg-white/[.035] p-7 sm:p-9">@csrf<div><label class="form-label">Full name</label><input name="customer_name" value="{{ old('customer_name', auth()->user()?->name) }}" class="form-input" required></div><div><label class="form-label">Email</label><input type="email" name="guest_email" value="{{ old('guest_email', auth()->user()?->email) }}" class="form-input" required></div><div><label class="form-label">Phone (optional)</label><input name="phone" value="{{ old('phone') }}" class="form-input"></div><div><label class="form-label">Delivery address</label><textarea name="delivery_address" rows="5" class="form-input" required>{{ old('delivery_address') }}</textarea></div>@if ($errors->any())<div class="rounded-2xl bg-rose-400/10 p-4 text-sm text-rose-200">Please correct the highlighted checkout information and try again.</div>@endif<button class="w-full rounded-full bg-lime-400 px-6 py-4 font-black text-black">Place pending order</button></form></main>
@endsection
